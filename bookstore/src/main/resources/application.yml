spring:
  application:
    name: book-service
  datasource:
    url: 'jdbc:h2:mem:mydb'
    username: sa
    password: pass
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  port: 8080